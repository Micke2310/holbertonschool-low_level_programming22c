#!/bin/bash
#include <stdio.h>

/*
 *main - Printe a statemet with proper grammar
 *fallowed by a new line and return 0. 
 * 
 *Return: Always 0 (Success)
 */
int main(void)
{
	printf("with propeer gramar, but the outcome is a place of art,\n");
	return (0);
}
