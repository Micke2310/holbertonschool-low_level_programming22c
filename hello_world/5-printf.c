#include <stdio.h>

/*
 *main - Printe a statemet with proper grammar
 *fallowed by a new line and return 0.
 *
 *Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piace of art,\n");
	return (0);
}
